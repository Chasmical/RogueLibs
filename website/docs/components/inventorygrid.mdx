# `InventoryGrid`

import { useState } from 'react';
import useBaseUrl from '@docusaurus/useBaseUrl';
import BrowserWindow from '@site/src/components/BrowserWindow';

import InventorySlot from '@site/src/components/InventorySlot';
import InventoryRow from '@site/src/components/InventoryRow';
import InventoryGrid from '@site/src/components/InventoryGrid';

### Props

```ts
export type Props = {
  items?: (SlotProps | SlotProps[])[], // alternatively, you can add slot and row props here
  children?: React.ReactNode,          // InventorySlot and InventoryRow children
  width?: number,                      // minimum width
  height?: number,                     // minimum height
  interactable?: boolean,              // determines whether it's interactable or not
  onChange?: (e: InventoryChangeProps) => void, // changed selected item handler
}
export type InventoryChangeProps = {
  index: number,  // item's index
  row: number,    // item's row index
  column: number, // item's column index
}
```

### Interactable
