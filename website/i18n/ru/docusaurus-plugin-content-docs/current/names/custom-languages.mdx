# Кастомные языки*

import Tabs from '@site/src/components/Tabs';
import TabItem from '@site/src/components/TabItem';
import useBaseUrl from '@docusaurus/useBaseUrl';

Вы можете добавлять свои языки в игру с помощью метода `LanguageService.RegisterLanguageCode`:

```csharp
LanguageService.RegisterLanguageCode("japanese", (LanguageCode)123);
```

:::info Work-In-Progress
:::

## `LanguageService` {#languageservice}

Статический класс `LanguageService` содержит всё что связанно с локализацией.

:::info Work-In-Progress
:::

## Добавление локализации в моды {#localizations}

:::info Work-In-Progress
:::